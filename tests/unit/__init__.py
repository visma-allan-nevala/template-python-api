"""
Unit Tests.

Fast, isolated tests that don't require external dependencies.
Mock all external services, databases, and caches.

Run unit tests only:
    make test-unit
    uv run pytest tests/unit -v
"""
